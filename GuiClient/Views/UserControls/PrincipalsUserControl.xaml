<UserControl x:Class="GuiClient.Views.UserControls.PrincipalsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GuiClient.ViewModels.UserControls"
             mc:Ignorable="d"
             d:DataContext="{ d:DesignInstance Type=vm:PrincipalsUserControlViewModel, IsDesignTimeCreatable=True }"
             d:DesignHeight="50">
    <StackPanel Orientation="Horizontal">
        <Button Content="Add principal" Style="{StaticResource RowControl}" Visibility="{Binding IsOwner}" />
        <Button Content="Delete principal" Style="{StaticResource RowControl}" Visibility="{Binding IsOwner}" />
        <Button Content="Change principal password (force)" Style="{StaticResource RowControl}"
                Visibility="{Binding IsOwner}" />
        <Button Content="Get principal by name" Style="{StaticResource RowControl}" Visibility="{Binding IsWorker}" />
        <Button Content="Get all principals" Style="{StaticResource RowControl}" Visibility="{Binding IsWorker}" />
    </StackPanel>
</UserControl>