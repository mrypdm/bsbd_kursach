<UserControl x:Class="GuiClient.Views.UserControls.TagsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="1000">
    <StackPanel Orientation="Horizontal">
        <Button Content="Add Tag" Style="{StaticResource RowControl}" />
        <Button Content="Delete Tag" Style="{StaticResource RowControl}" />
        <Button Content="Get Tag by ID" Style="{StaticResource RowControl}" />
        <Button Content="Get Tag by Name" Style="{StaticResource RowControl}" />
    </StackPanel>
</UserControl>