<UserControl x:Class="GuiClient.Views.UserControls.BooksToTagsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GuiClient.ViewModels.UserControls"
             mc:Ignorable="d"
             d:DataContext="{ d:DesignInstance Type=vm:BooksToTagsUserControlViewModel, IsDesignTimeCreatable=True }"
             d:DesignHeight="50">
    <StackPanel Orientation="Horizontal">
        <Button Content="Attach Tag to Book" Style="{StaticResource RowControl}" Visibility="{Binding IsWorker}" />
        <Button Content="Detach Tag from Book" Style="{StaticResource RowControl}" Visibility="{Binding IsWorker}" />
    </StackPanel>
</UserControl>