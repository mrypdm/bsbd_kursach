<Window x:Class="GuiClient.Views.Windows.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:GuiClient.ViewModels.Windows"
        mc:Ignorable="d"
        d:DataContext="{ d:DesignInstance Type=vm:BookWindowViewModel, IsDesignTimeCreatable=True }"
        x:Name="CurrentWindow"
        Title="{Binding WindowTitle}" d:Height="450" MinWidth="300" Style="{StaticResource WinAll}"
        SizeToContent="WidthAndHeight">
    <StackPanel>
        <StackPanel Margin="5">
            <TextBlock Text="Title" Margin="0,0,0,5" />
            <TextBox Text="{Binding Title}" TextAlignment="Center" MinHeight="15" />
        </StackPanel>

        <StackPanel Margin="5">
            <TextBlock Text="Author" Margin="0,0,0,5" />
            <TextBox Text="{Binding Author}" TextAlignment="Center" MinHeight="15" />
        </StackPanel>

        <StackPanel Margin="5">
            <TextBlock Text="Release date" Margin="0,0,0,5" />
            <DatePicker SelectedDate="{Binding ReleaseDate}" />
        </StackPanel>

        <StackPanel Margin="5">
            <TextBlock Text="Count" Margin="0,0,0,5" />
            <TextBox Text="{Binding Count}" TextAlignment="Center" MinHeight="15" />
        </StackPanel>

        <StackPanel Margin="5">
            <TextBlock Text="Price" Margin="0,0,0,5" />
            <TextBox Text="{Binding Price}" TextAlignment="Center" MinHeight="15" />
        </StackPanel>

        <StackPanel Margin="5">
            <TextBlock Text="Tags" Margin="0,0,0,5" />
            <TextBox Text="{Binding Tags}" TextAlignment="Center" MinHeight="15" />
        </StackPanel>

        <Button Content="Save" Margin="5"
                Command="{Binding Save}" CommandParameter="{Binding ElementName=CurrentWindow}" />
    </StackPanel>
</Window>