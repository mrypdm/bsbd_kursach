<UserControl x:Class="GuiClient.UserControls.OwnerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Height="50" Width="450">
    <StackPanel Orientation="Horizontal">
        <Button Content="Add user" Style="{StaticResource RowControl}" />
        <Button Content="Delete user" Style="{StaticResource RowControl}" />
        <Button Content="Change user password (force)" Style="{StaticResource RowControl}" />
    </StackPanel>
</UserControl>