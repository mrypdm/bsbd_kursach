<UserControl x:Class="GuiClient.UserControls.ClientsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Button Content="Add Client" Style="{StaticResource RowControl}" />
        <Button Content="Delete Client" Style="{StaticResource RowControl}" />
        <Button Content="Get Client by ID" Style="{StaticResource RowControl}" />
        <Button Content="Get Client by Name" Style="{StaticResource RowControl}" />
        <Button Content="Get Client by Phone" Style="{StaticResource RowControl}" />
    </StackPanel>
</UserControl>
